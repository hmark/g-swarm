<!ACT> = fire(<!FIR>); | if(<!ANG> > <!ANX>){<!ACT>}else{<!ACT>} | if(<!ERG> > <!ERX>){<!ACT>}else{<!ACT>} | if(<!SCR> > <!SCX>){<!ACT>}else{<!ACT>} | turnGunLeft(<!ANG>); | turnLeft(<!ANG>); | ahead(<!SCX>); | back(<!SCX>); | doNothing();
<!ERG> = getEnergy() | e.getEnergy()
<!ERX> = getEnergy() | e.getEnergy() | 25 | 50 | 75 | 100
<!ANG> = getHeading() | getGunHeading() | getRadarHeading() | e.getHeading() | e.getBearing()
<!ANX> = getHeading() | getGunHeading() | getRadarHeading() | e.getHeading() | e.getBearing() | 90 | 180 | 270 | 360
<!SCR> = getX() | getY() | e.getDistance()
<!SCX> = getX() | getY() | e.getDistance() | 0 | 100 | 150 | 200 | 250 | 300 | 350 | 400 | 450 | 500 | 550 | 600
<!FIR> = 1 | 2 | 3
